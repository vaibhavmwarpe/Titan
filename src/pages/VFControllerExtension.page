<apex:page standardController="Account" extensions="VFContactExtension">
    <p>{!title}</p>
    <apex:form >
        <apex:inputField value="{!account.name}"/>
        <apex:commandButton action="{!save}" value="Save!"/>
    </apex:form>
</apex:page>